import {useEffect,useState} from 'react'; import {useParams} from 'react-router-dom'; import {http} from '../api/http'; import {useAuth} from '../contexts/AuthContext'; export default function ProjectDetail(){const {id}=useParams();const {user}=useAuth();const [p,setP]=useState<any>();const [devs,setDevs]=useState<any[]>([]);const [title,setTitle]=useState('');const [developerId,setDeveloperId]=useState('');const [priority,setPriority]=useState('MEDIUM');const [dueDate,setDueDate]=useState('');const load=()=>http.get(`/projects/${id}`).then(r=>setP(r.data.data));useEffect(()=>{load();if(user?.role!=='DEVELOPER')http.get('/users').then(r=>setDevs(r.data.data.filter((u:any)=>u.role==='DEVELOPER')))},[id]);if(!p)return <section><p>Loading…</p></section>;async function create(e:any){e.preventDefault();await http.post(`/projects/${id}/tasks`,{title,developerId:developerId||null,priority,dueDate,status:'TODO'});setTitle('');load()}return <section><h1>{p.name}</h1><p>{p.description}</p>{user?.role!=='DEVELOPER'&&<form className="inlineForm" onSubmit={create}><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Task title" required/><select value={developerId} onChange={e=>setDeveloperId(e.target.value)}><option value="">Unassigned</option>{devs.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select><select value={priority} onChange={e=>setPriority(e.target.value)}><option>LOW</option><option>MEDIUM</option><option>HIGH</option><option>CRITICAL</option></select><input type="date" value={dueDate} onChange={e=>setDueDate(e.target.value)} required/><button>Create task</button></form>} {p.tasks.map((t:any)=><div className="task" key={t.id}><b>#{t.id} {t.title}</b><span>{t.status}</span><span>{t.priority}</span><span>{t.developer?.name||'Unassigned'}</span><span>{new Date(t.dueDate).toLocaleDateString()}</span></div>)}</section>}
