import {useEffect,useState} from 'react'; import {http} from '../api/http'; import {useAuth} from '../contexts/AuthContext'; export default function Dashboard(){const {user}=useAuth();const [data,setData]=useState<any>(null);useEffect(()=>{if(user)http.get(`/dashboard/${user.role==='ADMIN'?'admin':user.role==='PROJECT_MANAGER'?'manager':'developer'}`).then(r=>setData(r.data.data))},[user]);if(!data)return <section><h1>Dashboard</h1><p>Loading…</p></section>;if(user?.role==='ADMIN')return <section><h1>Admin Dashboard</h1><div className="cards"><Card t="Projects" v={data.totalProjects}/><Card t="Tasks" v={data.totalTasks}/><Card t="Overdue" v={data.overdueTaskCount}/><Card t="Online" v={data.onlineUsers}/></div><h3>Tasks by status</h3><pre>{JSON.stringify(data.tasksByStatus,null,2)}</pre></section>;if(user?.role==='PROJECT_MANAGER')return <section><h1>Manager Dashboard</h1><div className="cards">{Object.entries(data.tasksByPriority).map(([k,v])=><Card key={k} t={k} v={v as number}/>)}</div><h3>Projects</h3>{data.projects.map((p:any)=><div className="row" key={p.id}><b>{p.name}</b><span>{p.tasks.length} tasks</span></div>)}</section>;return <section><h1>Developer Dashboard</h1>{data.tasks.map((t:any)=><div className="row" key={t.id}><b>#{t.id} {t.title}</b><span>{t.priority} · {t.status} · {new Date(t.dueDate).toLocaleDateString()}</span></div>)}</section>}function Card({t,v}:{t:string,v:number}){return <div className="card"><span>{t}</span><strong>{v}</strong></div>}
