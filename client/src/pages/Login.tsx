import {useState} from 'react'; import {useAuth} from '../contexts/AuthContext'; import {useNavigate} from 'react-router-dom'; export default function Login(){const {login}=useAuth();const nav=useNavigate();const [email,setEmail]=useState('admin@example.com'),[password,setPassword]=useState('Password123!'),[err,setErr]=useState('');return <div className="login"><form onSubmit={async e=>{e.preventDefault();try{await login(email,password);nav('/dashboard')}catch(x:any){setErr(x.response?.data?.error?.message||'Login failed')}}}><h1>Client Project Dashboard</h1><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/><button>Sign in</button>{err&&<p className="error">{err}</p>}</form></div>}
