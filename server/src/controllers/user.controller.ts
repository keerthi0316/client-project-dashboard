import {Response} from 'express'; import bcrypt from 'bcryptjs'; import {prisma} from '../config/prisma'; import {AuthRequest} from '../types/auth'; import {AppError} from '../utils/errors'; export async function listUsers(_req:AuthRequest,res:Response){res.json({success:true,data:await prisma.user.findMany({select:{id:true,name:true,email:true,role:true},orderBy:{name:'asc'}})})} export async function createUser(req:AuthRequest,res:Response){const {name,email,password,role}=req.body;if(!name||!email||!password||!role)throw new AppError(400,'VALIDATION_ERROR','name, email, password and role are required');const exists=await prisma.user.findUnique({where:{email}});if(exists)throw new AppError(409,'EMAIL_EXISTS','Email already exists');const user=await prisma.user.create({data:{name,email,passwordHash:await bcrypt.hash(password,12),role},select:{id:true,name:true,email:true,role:true}});res.status(201).json({success:true,data:user})} export async function updateUser(req:AuthRequest,res:Response){const user=await prisma.user.findUnique({where:{id:String(req.params.id)}});if(!user)throw new AppError(404,'USER_NOT_FOUND','User not found');const data:any={};if(req.body.name)data.name=req.body.name;if(req.body.email)data.email=req.body.email;if(req.body.role)data.role=req.body.role;if(req.body.password)data.passwordHash=await bcrypt.hash(req.body.password,12);const updated=await prisma.user.update({where:{id:user.id},data,select:{id:true,name:true,email:true,role:true}});res.json({success:true,data:updated})}
