import {Router} from 'express'; import {listTasks,updateStatus} from '../controllers/task.controller'; import {authenticate} from '../middleware/auth'; import {validate} from '../middleware/validate'; import {statusSchema} from '../validators/schemas'; const r=Router();r.use(authenticate);r.get('/',listTasks);r.patch('/:id/status',validate(statusSchema),updateStatus);export default r;
