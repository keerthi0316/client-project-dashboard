import {Router} from 'express'; import {adminDashboard,managerDashboard,developerDashboard} from '../controllers/dashboard.controller'; import {authenticate,authorize} from '../middleware/auth'; const r=Router();r.get('/admin',authenticate,authorize('ADMIN'),adminDashboard);r.get('/manager',authenticate,authorize('PROJECT_MANAGER'),managerDashboard);r.get('/developer',authenticate,authorize('DEVELOPER'),developerDashboard);export default r;
