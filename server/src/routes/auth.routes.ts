import {Router} from 'express'; import {loginController,refreshController,logoutController} from '../controllers/auth.controller'; import {validate} from '../middleware/validate'; import {loginSchema} from '../validators/schemas'; const r=Router();r.post('/login',validate(loginSchema),loginController);r.post('/refresh',refreshController);r.post('/logout',logoutController);export default r;
