import {z} from 'zod'; export const loginSchema=z.object({email:z.string().email(),password:z.string().min(8)}); export const projectSchema=z.object({name:z.string().min(2).max(120),description:z.string().max(2000).optional(),clientId:z.string().uuid()}); export const taskSchema=z.object({title:z.string().min(2).max(200),description:z.string().max(5000).optional(),developerId:z.string().uuid().nullable().optional(),status:z.enum(['TODO','IN_PROGRESS','IN_REVIEW','DONE','OVERDUE']).default('TODO'),priority:z.enum(['LOW','MEDIUM','HIGH','CRITICAL']).default('MEDIUM'),dueDate:z.coerce.date()}); export const statusSchema=z.object({status:z.enum(['TODO','IN_PROGRESS','IN_REVIEW','DONE'])}); export const filtersSchema=z.object({status:z.enum(['TODO','IN_PROGRESS','IN_REVIEW','DONE','OVERDUE']).optional(),priority:z.enum(['LOW','MEDIUM','HIGH','CRITICAL']).optional(),from:z.coerce.date().optional(),to:z.coerce.date().optional()});
